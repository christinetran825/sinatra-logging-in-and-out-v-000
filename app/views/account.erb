<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
      <h1>Welcome <%=Helpers.current_username(session).username%> </h1>
      <h3>Your Balance: <%=Helpers.current_username(session).balance%> </h3>
      <br>
      <p><a href="/logout">Log Out</a></p>
    <% end %>
  </body>
</html>
